# Cosmic-Api — Prototype

Quickstart:

1. `git clone` or create folder and add the files.
2. `cd Cosmic-Api`
3. `npm install`
4. `npm run dev` (requires nodemon) or `npm start`.

POST endpoint:
`POST http://localhost:3000/api/chart`
Body (JSON):
